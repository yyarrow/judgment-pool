// ChatPanel is no longer used — its functionality has been integrated directly
// into TaskDetailPage (thread-view style). This stub is kept to avoid import
// errors in any files that may still reference it.
export default function ChatPanel() {
  return null
}
